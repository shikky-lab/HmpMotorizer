<?xml version="1.0" encoding="utf-8"?>

<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="200dp"
    android:layout_height="52dp">

    <ImageButton
        android:id="@+id/direction_right_button"
        android:layout_width="100dp"
        android:layout_height="52dp"
        android:background="@drawable/bg_border"
        android:scaleType="fitXY"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/direction_zigzag_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_print_right" />

    <ImageButton
        android:id="@+id/direction_zigzag_button"
        android:layout_width="100dp"
        android:layout_height="52dp"
        android:layout_marginEnd="16dp"
        android:background="#000000"
        android:scaleType="fitXY"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/direction_right_button"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_print_zigzag" />

</android.support.constraint.ConstraintLayout>